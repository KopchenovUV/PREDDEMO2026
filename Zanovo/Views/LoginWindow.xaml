<Window x:Class="Zanovo.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация - Информационная система" 
        Height="650" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Вход в систему" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Логин -->
        <StackPanel Grid.Row="1" Margin="0,5">
            <TextBlock Text="Логин:" FontSize="14" Margin="0,0,0,5"/>
            <TextBox x:Name="txtLogin" Height="35" FontSize="14" Padding="5"/>
        </StackPanel>

        <!-- Пароль -->
        <StackPanel Grid.Row="2" Margin="0,5">
            <TextBlock Text="Пароль:" FontSize="14" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtPassword" Height="35" FontSize="14" Padding="5"/>
        </StackPanel>

        <!-- Капча -->
        <StackPanel Grid.Row="3" Margin="0,15">
            <TextBlock Text="Соберите пазл (кликайте на фрагменты в порядке 1→2→3→4):" 
                       FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"/>
            <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" 
                    HorizontalAlignment="Center" Padding="5">
                <Grid x:Name="CaptchaContainer" Width="210" Height="210">
                    <TextBlock Text="Загрузка..." VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Grid>
            </Border>
        </StackPanel>

        <!-- Кнопка входа -->
        <Button x:Name="btnLogin" Grid.Row="4" Content="Войти" 
                FontSize="16" FontWeight="Bold" Height="40" Width="150"
                HorizontalAlignment="Center" Click="BtnLogin_Click"
                Background="#007ACC" Foreground="White" 
                Cursor="Hand" Margin="0,20"/>

        <!-- Сообщение об ошибке -->
        <TextBlock x:Name="tbError" Grid.Row="5" Foreground="Red" 
                   TextWrapping="Wrap" Margin="0,10" 
                   FontSize="12" Visibility="Collapsed"/>

        <!-- Подсказка -->
        <TextBlock Grid.Row="6" Text="Подсказка: кликните на фрагмент с цифрой 1, затем 2, затем 3, затем 4" 
                   FontSize="10" Foreground="Gray" 
                   TextWrapping="Wrap" HorizontalAlignment="Center"/>
    </Grid>
</Window>